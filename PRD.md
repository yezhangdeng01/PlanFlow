# PlanBoard — Obsidian 计划体系插件（PRD v1.1）

> 个人计划管理工具：计划 → 任务 → 行动 三层体系 + 进度统计 + 目标管理，清爽卡片风格。
> 定位：个人工具 → 社区发布候选（数据纯 markdown，不锁定）。

## 0. 概念模型（v1.1 新增，核心）

| 层级 | 名称 | 定义 | 存储 | 例子 |
|---|---|---|---|---|
| 🎯 计划 | 年度建立，有目标 | 名称+类型+目标值+描述 | 年度计划笔记 | 写作 → 12 篇公众号文章 |
| 📋 任务 | 从计划分解到月/周 | 内容+关联计划+日期 | 任务池 `{root}/{year}/任务.md` | 完成《霍去病》文章 |
| ✅ 行动 | 每日例行打卡 | 固定项 | 每日笔记 | 写作 1 小时 |

**规则**：
- 计划分解到任务为最低粒度（12 篇 → 每篇一个任务），不分解到日
- 任务显示在本周/本月视图，勾选即完成
- 数量型计划进度 = 关联任务完成数（自动推进）；打卡型计划进度 = 行动打卡率（自动）
- 任务池统一存储（Tasks 格式，`#计划/标签` + 🛫📅 日期），今日/周/月视图按日期窗口过滤，看板按状态分组，甘特按日期排列

## 1. 产品概述

一个 Obsidian 插件，提供一个「计划总览」视图（Homepage），聚合：
- **今日**：打卡勾选（实时写回）、今日进度、总结页内编辑、复盘入口、今日到期临时任务
- **本周 / 本月 / 年度**：各计划打卡率进度条、目标、临时任务
- 全部基于用户的 markdown 笔记（Tasks 任务格式 + frontmatter），不做私有数据存储

## 2. 数据模型（核心，必须严格遵守）

### 2.1 目录结构（默认值，设置页可改根目录）

```
{root}/                       ← 默认 raw/计划/
├── 年度计划.md               ← 年度目标定义
├── {year}/                   ← 2026/
│   ├── 月/2026-08.md         ← 月度：目标 + temp-tasks
│   ├── 周/2026-W33.md        ← 周度：目标 + temp-tasks
│   └── 每日/2026-08-11.md    ← 每日：打卡任务 + 总结
```

### 2.2 每日笔记格式（模板自动生成，插件读写同一格式）

```markdown
---
date: 2026-08-11
type: daily
---

# 📅 2026-08-11 星期二

## ✅ 今日打卡
- [ ] ✍️ 写作 1小时 #计划/写作 🛫 2026-08-11 📅 2026-08-11
- [ ] 🏃 健康 1小时 #计划/健康 🛫 2026-08-11 📅 2026-08-11
- [ ] 📖 学习 1小时 #计划/学习 🛫 2026-08-11 📅 2026-08-11
- [ ] 📈 复盘 + 次日交易计划 → [[2026-08-11 复盘]] #计划/复盘 🛫 2026-08-11 📅 2026-08-11

## 📝 今日总结
- **今日完成**：
- **未完成**：
- **感想/收获**：
```

- 打卡项 = Tasks 标准任务行：`- [ ] 内容 #计划/{计划名} 🛫 YYYY-MM-DD 📅 YYYY-MM-DD`
- 计划名集合：`写作 / 健康 / 学习 / 复盘`（设置页可改）
- 总结区：固定标题 `## 📝 今日总结`，下方为 markdown 内容（插件编辑时替换标题与下一个 `##` 标题之间的内容）
- 临时任务（手动加）：同一文件的 `- [ ] 内容 📅 日期`（不带 #计划/ 标签，不计入计划进度）

### 2.3 月/周笔记

```markdown
---
type: monthly
period: 2026-08
temp-tasks: |
  - [ ] 完成《霍去病》初稿 📅 2026-08-20
---
# 🗓️ 2026-08 月度目标
- **重点**：写作 4 篇、跑步 20 天
...
```

- `temp-tasks`：frontmatter 多行块（Tasks 格式），每日到期任务自动在「今日」显示
- 周笔记 `type: weekly, period: 2026-W33`

### 2.4 统计口径（必须与旧体系一致）

- **计划打卡率（周/月/年）** = 该计划在周期内勾选打卡的天数 ÷ 周期内应有天数
  - 写作/健康/学习：应有天数 = 周期总天数（周 7、月 = 当月天数、年 = 已过天数）
  - 复盘：应有天数 = 周期内工作日数（周 5、月 = 当月工作日）
  - 年视图：分母 = 年初至今天数（已过天数，不预测未来）
- **临时任务完成率** = 已完成任务数 ÷ 任务总数（按 period 过滤）
- **今日进度** = 今日已勾选打卡项 ÷ 今日打卡项总数

## 3. 视图规格

### 3.1 今日视图（最重要，MVP 核心）
- **打卡卡片**：4 项默认打卡（可增删/排序），checkbox 点击 → 立即写回文件 → 进度条动画更新
- **今日进度条**：已勾选/总数，大数字显示百分比
- **总结编辑**：textarea 直接编辑今日总结，**失焦自动保存**（无保存按钮）；未写时显示占位提示
- **复盘入口**：按钮「写复盘」→ 打开/创建 `{root}/{year}/每日/{date} 复盘.md`
- **临时任务**：今日到期的 temp-tasks 列表（来自月/周笔记），可勾选完成
- **添加打卡项**：+ 按钮 → 输入名称/时长/选计划 → 写入今日笔记

### 3.2 周视图
- 各计划进度条卡片（打卡率 + 打卡天数/应有天数）
- 本周目标（读周笔记正文）
- 本周临时任务列表

### 3.3 月视图 / 年度视图
- 同周视图结构，周期换为月/年
- 年度视图：4 个计划各一个大卡片 + 年度目标

### 3.4 视图切换
- 顶部 tab：今日 / 本周 / 本月 / 年度
- 图标注册到左侧边栏（📅），命令 `PlanBoard: 打开计划总览`（Ctrl+P 可调）
- 设置项可开关「启动时自动打开」

## 4. 交互规格

| 操作 | 行为 |
|---|---|
| 勾选打卡 | 写回文件（`- [ ]` ↔ `- [x]`），进度条 200ms 动画，不刷新页面 |
| 文件外部改动 | 监听 `vault.modify` + 防抖 500ms，刷新当前视图（实时联动） |
| 编辑总结 | textarea 失焦 → 解析文件 → 替换总结区 → 写回 → 视图刷新 |
| 添加打卡项 | 弹窗（Modal）：名称 + 计划选择 + 时长 → 追加到今日笔记 |
| 删除打卡项 | 悬停显示 ✕ → 删除该行 |
| 打卡项排序 | 拖拽或 ↑↓ 按钮（MVP 用 ↑↓ 按钮，拖拽二期） |
| 打开复盘 | 存在则打开，不存在则用模板创建 |

## 5. 视觉规范（清爽卡片，美学优先）

- **卡片**：圆角 10px、浅色模式 `#ffffff` 卡片 + `#f5f6f8` 背景、柔和阴影（`0 1px 3px rgba(0,0,0,0.06)`）
- **深色模式**：完整适配，卡片 `#1e1e1e` / 背景 `#171717`，文本用 CSS 变量（`var(--text-normal)` 等）
- **色板**（低饱和）：进度绿 `#10b981`、进度蓝 `#3b82f6`、逾期暖红 `#ef4444`、中性灰 `#9ca3af`
- **进度条**：圆角、渐变（绿→蓝）、百分比数字加粗加大（20px）
- **计划图标色**：写作 `#f59e0b`、健康 `#10b981`、学习 `#3b82f6`、复盘 `#ef4444`（可配置）
- **排版**：标题 16px semibold，正文 14px，卡片间距 12px，留白充足
- 全部用 Obsidian CSS 变量，跟随主题；不使用内联 style（用 CSS 类）

## 6. 设置页（克制但完整）

| 设置项 | 默认值 | 说明 |
|---|---|---|
| 计划根目录 | `raw/计划` | 笔记存放根 |
| 每日打卡项模板 | 写作 1h/健康 1h/学习 1h/复盘 | 可增删改（名称+时长+计划名+是否含复盘链接） |
| 复盘按工作日统计 | 开 | 复盘打卡率分母用工作日 |
| 启动时打开 PlanBoard | 开 | 启动后自动打开今日视图 |
| 图标主题 | 📅 | 侧边栏图标 |

## 7. 技术约束

- TypeScript + esbuild（Obsidian 官方模板结构）
- Obsidian 最低版本：`1.5.0`（manifest.minAppVersion），API 调用尽量用稳定接口
- 不依赖其他插件（自包含）；读 Tasks 格式但不要求安装 Tasks 插件
- 文件写入用 `vault.process()`（保留文件其余内容，只改目标行）
- 视图用 `ItemView`（`VIEW_TYPE_PLANBOARD`），`getViewType`/`getDisplayText`/`getIcon` 标准实现
- 所有文案中文（代码注释英文）

## 8. 里程碑

- **M1**：项目搭建 + 今日视图（打卡勾选实时写回 + 进度 + 总结编辑 + 复盘入口）→ 验收：日常使用不再别扭
- **M2**：周/月/年度视图 + 设置页 + 视觉打磨（深色模式）→ 验收：愿意每天打开
- **M3**：轻量甘特图 + 临时任务管理增强 + 边界完善（跨年、补卡、缺文件容错）→ 验收：一个月稳定
- **M4（社区）**：README + 演示 GIF + 发布审核材料 + 中英文案

## 9. 验收标准（用户视角）

1. 打开 Obsidian → PlanBoard 自动出现，今日打卡一眼可见
2. 勾选打卡 → 进度条立即变化，**不需要刷新**
3. 写总结 → 页内直接写，离开自动保存
4. 切周/月/年度 → 进度数据准确（与旧体系口径一致）
5. 深色/浅色模式都好看
6. 增删打卡项、改配置 → 操作直观，即时生效
